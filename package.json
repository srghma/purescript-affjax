{
  "private": true,
  "scripts": {
    "build": "eslint src && spago build --censor-stats --strict --pedantic-packages"
  },
  "devDependencies": {
    "body-parser": "^1.20.3",
    "eslint": "^9.12.0",
    "express": "^4.21.1",
    "purescript-psa": "^0.9.0",
    "xhr2": "^0.2.1"
  }
}
